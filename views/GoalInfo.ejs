<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Goal Details</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Fitness Goal Setting App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addGoal">Add New Goal</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/bmi">BMI Calculator</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact Us</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container my-4">
        <h2 class="text-center">Goal Details</h2>

        <% if (goal) { %> <!-- Check if goal data exists -->
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title">Goal ID: <%= goal.goal_id %></h5>
                    <p class="card-text"><b>Goal Type:</b> <%= goal.goal_type %></p>
                    <% if (goal.image) { %>
                        <img src="/images/<%= goal.image %>" class="img-fluid mb-3" alt="Goal Image">
                    <% } %>
                    <p class="card-text"><b>Description:</b> <%= goal.description %></p>
                    <p class="card-text"><b>Target Days:</b> <%= goal.target_days %></p>
                    <p class="card-text"><b>Percentage of Achievement:</b> <%= goal.percentage_of_achievement %>%</p>

                    <!-- Display the custom message based on percentage_of_achievement -->
                    <div class="alert alert-info">
                        <% if (goal.percentage_of_achievement > 75) { %>
                            You are almost there!
                        <% } else if (goal.percentage_of_achievement > 50) { %>
                            Keep up the great work!
                        <% } else { %>
                            Keep working on your goal!
                        <% } %>
                    </div>
                </div>
            </div>
        <% } else { %>
            <p>No goal found.</p>
        <% } %>
    </div>
</body>
</html>
