<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Fitness Goal Setting App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Fitness Goal Setting App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/addGoal">Add New Goal</a>
          </li>
          <li class="nav-item">
              <a class="nav-link" href="/bmi">BMI Calculator</a>
          </li>
          <li class="nav-item">
              <a class="nav-link" href="/contact">Contact Us</a>
            </li> 
          </ul>
        </div>
      </div>
    </nav>
  <div class="container">
    <br>
    <div class="text-center"><h2>Goals from Goal Setting App Database</h2></div>
    <br>
  
    <table class="table table-hover small text-center">
      <thead>
          <tr>
            <th width="100">Goal Type</th>
            <th width="50">Image</th>
            <th width="50">Description</th>
            <th width="50">Target Days</th>
            <th width="50">Percentage Of Achievement</th>
            <th width="50">Edit</th>
            <th width="50">Delete</th>
          </tr>
      </thead>
      <tbody>
        <% for(let i=0; i < goals.length; i++) { %>
          <tr>
            <td><%=goals[i].goal_type %></td>
            <td><img src = "images/<%= goals[i].image %>" width="20%"></td>
            <td><%= goals[i].description %></td>
            <td><%= goals[i].target_days %></td>
            <td><%= goals[i].percentage_of_achievement %></td>
            <td><a href="/updateGoal/<%= goals[i].goal_id %>">Edit</a></td>
            <td><a href="/deleteGoal/<%= goals[i].goal_id %>" onclick="return confirm('Are you sure to delete?')">Delete</a></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>