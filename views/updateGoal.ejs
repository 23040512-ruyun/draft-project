<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Update Goal</title>
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Update Goal</h2>
        <form action="/updateGoal/<%= goal.goal_id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3 mt-3">
                <label for="goal_type">Goal Type:</label>
                <input type="text" class="form-control" id="goal_type" value="<%= goal.goal_type %>" name="goal_type">
            </div>
            <div class="mb-3 mt-3">
                <label for="description">Description:</label>
                <input type="text" class="form-control" id="description" value="<%= goal.description %>" name="description">
            </div>
            <div class="mb-3 mt-3">
                <label for="target_days">Target Days:</label>
                <input type="text" class="form-control" id="target_days" value="<%= goal.target_days %>" name="target_days">
            </div>
            <div class="mb-3 mt-3">
                <label for="percentage_of_achievement">Percentage of Achievement:</label>
                <input type="text" class="form-control" id="percentage_of_achievement" value="<%= goal.percentage_of_achievement %>" name="percentage_of_achievement">
            </div>
            <div class="mb-3 mt-3">
                <label for="image">Image:</label>
                <input type="file" class="form-control" id="image" name="image">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>
</html>
